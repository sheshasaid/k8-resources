apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080    # ✔ correct field
      nodePort: 30080
  selector:
    app: myapp
